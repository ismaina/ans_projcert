---
- hosts: puppet
  gather_facts: False
  become: true
  vars:
    - packages: ["git"]
  tasks:
    - name: Installing {{ packages }} on {{ hostname }}
      apt:
        name: "{{ item }}"
        state: present
      with_items: "{{ packages }}"